<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Employee</title>
    <link rel="stylesheet" href="/styles2.css"> <!-- Assuming you saved the CSS in styles1.css -->
</head>
<body>
    <div class="container">
        <div id="edit-employee-panel">
            <h1>Edit Employee</h1>
            <form action="/edit-employee/<%= employee.id %>" method="POST">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" value="<%= employee.name %>" required><br>
                <label for="salary">Salary:</label>
                <input type="text" id="salary" name="salary" value="<%= employee.salary %>" required><br>
                <button type="submit">Save Changes</button>
            </form>
            <a href="/employees">Back to Employee List</a>
        </div>
    </div>
</body>
</html>
